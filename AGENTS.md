# AGENTS.md

## Cursor Cloud specific instructions

This repository ("cursory") is currently an empty scaffold — it contains only a `README.md`.

- **No build system, package manager, or dependencies** are present.
- **No services** need to be started.
- **No lint, test, or build commands** are available yet.

When source code and a build system are added, update this section with:
1. How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
2. How to run lint, tests, and the dev server.
3. Any non-obvious environment caveats discovered during setup.
